# demo1
practice
Quick picks (short):

General image analysis (labels, tags, OCR, read text, describe image) → Computer Vision (prebuilt APIs). 
pluralsight.com
+1

Custom image classifier or object detector (aapke khud ke labels & training data) → Custom Vision (train your own model with your images). 
Microsoft Learn
+1

Faces: detect, verify, group, face attributes → Face API (agar sirf face-specific features chahiye). 
SpotSaaS

Structured documents, invoices, receipts, forms, tables → Document Intelligence / Form Recognizer (prebuilt + custom models for extracting fields & tables). 
Microsoft Learn
+1
